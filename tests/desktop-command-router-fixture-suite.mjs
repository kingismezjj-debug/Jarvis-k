console.error(
  "desktop-command-router-fixture-suite.mjs is disabled because fixture router replay is no longer part of the production desktop smoke path. Use npm run test:desktop:command-router-fixture for explicit test-only fixture coverage."
);
console.error("COMMAND_ROUTER_FIXTURE_SMOKE_ALIAS_DISABLED");
process.exit(1);
